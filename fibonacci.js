const prompt = require('prompt-sync')({ signit : true });

const input = prompt('Enter a value for [n]:'); 
let n = 0;
let x = 0; 
let y = 1;

while (Number !== 'done' ) {
    n = x + y; 
}

console.log(n);